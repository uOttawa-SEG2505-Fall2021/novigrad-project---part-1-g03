version: 2
jobs:
 test:
   docker:
     - image: circleci/android:api-29
   steps:
     - checkout
     - run:
         name: Grant gradle permissions
         command: sudo chmod +x ./gradlew
     - run:
         name: Download Dependencies
         command: ./gradlew androidDependenciesworkflows: